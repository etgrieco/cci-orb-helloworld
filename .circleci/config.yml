version: 2.1
orbs:
  hello: etgrieco/hello-world@dev:0.0.1

workflows:
  orb-test:
    jobs:
      - hello/pyscript
    triggers:
      - schedule:
          cron: "* * * * *"
          filters:
              branches:
                only:
                  - master
